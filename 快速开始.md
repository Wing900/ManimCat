# ManimGo 快速开始指南 🚀

**3 步启动你的视频生成服务！**

---

## 第一步：安装 Redis (只需一次)

### 推荐方式：Memurai (2 分钟完成)

1. **下载 Memurai**
   - 访问: https://www.memurai.com/get-memurai
   - 选择 "Memurai Developer" (免费)
   - 下载安装包 (约 10MB)

2. **安装**
   ```
   ✅ 双击 .msi 安装包
   ✅ 勾选 "Add to PATH"
   ✅ 勾选 "Install as Service"
   ✅ 点击 "Install"
   ```

3. **验证**
   ```cmd
   memurai-cli ping
   ```
   看到 `PONG` 就成功了！

**详细说明**: 查看 [`Windows安装Redis指南.md`](Windows安装Redis指南.md:1)

---

## 第二步：配置项目 (只需一次)

### 1. 安装依赖
```cmd
npm install
```

### 2. 配置环境变量
复制 `.env.example` 为 `.env`:
```cmd
copy .env.example .env
```

编辑 `.env` 文件，填入你的 OpenAI API Key:
```env
OPENAI_API_KEY=sk-你的key
OPENAI_MODEL=gpt-4.1-nano
NODE_ENV=development
```

---

## 第三步：启动服务

### 方式 A：双击启动 (最简单) ⭐

**直接双击** [`启动ManimGo.bat`](启动ManimGo.bat:1)

脚本会自动：
- ✅ 检查 Node.js
- ✅ 检查 Redis
- ✅ 启动 Redis 服务
- ✅ 启动 ManimGo 服务器

### 方式 B：命令行启动

```cmd
npm run dev
```

---

## 验证运行

### 1. 访问服务
浏览器打开: http://localhost:3000

### 2. 健康检查
```cmd
curl http://localhost:3000/health
```

应该看到:
```json
{
  "status": "healthy",
  "redis": {
    "status": "connected"
  },
  "queue": {
    "waiting": 0,
    "active": 0
  }
}
```

### 3. 测试生成视频
```cmd
curl -X POST http://localhost:3000/api/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"concept\":\"勾股定理\",\"quality\":\"low\"}"
```

---

## 常见问题

### ❓ Redis 连接失败？

**检查 Redis 是否运行**:
```cmd
memurai-cli ping
```

**启动 Redis 服务**:
```cmd
net start memurai
```

### ❓ 端口 3000 被占用？

**方式一：修改端口**
编辑 `.env`:
```env
PORT=3001
```

**方式二：停止占用进程**
```cmd
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### ❓ OpenAI API 调用失败？

1. 检查 API Key 是否正确
2. 检查网络连接
3. 确认 API 额度充足

---

## 目录说明

```
ManimGo/
├── 启动ManimGo.bat          # 一键启动脚本 ⭐
├── Windows安装Redis指南.md  # Redis 安装详解
├── 快速开始.md              # 本文档
├── 如何运行.md              # 详细运行说明
├── MRE验收报告.md           # 重构验收报告
├── package.json            # 项目配置
├── .env.example            # 环境变量模板
├── src/                    # 源代码
│   ├── server.ts          # Express 入口
│   ├── config/            # 配置文件
│   ├── routes/            # API 路由
│   ├── queues/            # 任务处理器
│   └── services/          # 业务服务
└── public/videos/         # 生成的视频
```

---

## 下一步

### 开发相关
- 📝 查看 [`Part1-接口文档.md`](Part1-接口文档.md:1) 了解 API 规范
- 🔧 修改 `src/` 下的代码进行开发
- 🧪 运行测试 (即将支持)

### 部署相关
- 🐳 Docker 部署: 查看 [`如何运行.md`](如何运行.md:1)
- 📦 生产构建: `npm run build`
- 🚀 启动生产: `npm start`

---

## 核心命令速查

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm start` | 启动生产服务器 |
| `memurai-cli ping` | 测试 Redis 连接 |
| `net start memurai` | 启动 Redis 服务 |

---

## 获取帮助

- 📖 详细文档: [`如何运行.md`](如何运行.md:1)
- 🔧 Redis 问题: [`Windows安装Redis指南.md`](Windows安装Redis指南.md:1)
- 📋 验收报告: [`MRE验收报告.md`](MRE验收报告.md:1)

---

**祝你使用愉快！** 🎉